# ABOUTME: Docker Compose configuration for running fold-gateway
# ABOUTME: Supports both direct TCP and Tailscale (tsnet) modes

services:
  # Standard mode: direct TCP ports
  fold-gateway:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fold-gateway
    ports:
      - "50051:50051"  # gRPC
      - "8080:8080"    # HTTP API
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - ./data:/app/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  # Tailscale mode: tsnet networking (no exposed ports needed)
  # Usage: docker compose up fold-gateway-tailscale
  fold-gateway-tailscale:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fold-gateway-ts
    profiles:
      - tailscale
    environment:
      - TS_AUTHKEY=${TS_AUTHKEY:-}  # Set via env or .env file
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - ./data:/app/data
      - tailscale-state:/app/tailscale  # Persist tsnet state
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:80/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s  # Longer start for Tailscale auth

volumes:
  tailscale-state:
