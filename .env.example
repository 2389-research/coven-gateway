# ABOUTME: Example environment variables for fold-gateway deployment
# ABOUTME: Copy to .env and fill in your values

# =============================================================================
# Tailscale (required for tsnet mode)
# =============================================================================
# Generate at: https://login.tailscale.com/admin/settings/keys
# Use a reusable key for persistent deployments
TS_AUTHKEY=tskey-auth-xxxxx

# =============================================================================
# Slack Frontend (optional)
# =============================================================================
# Socket mode app token (starts with xapp-)
SLACK_APP_TOKEN=xapp-1-xxxxx
# Bot user OAuth token (starts with xoxb-)
SLACK_BOT_TOKEN=xoxb-xxxxx

# =============================================================================
# Matrix Frontend (optional - for fold-matrix bridge)
# =============================================================================
MATRIX_USERNAME=@yourbot:matrix.org
MATRIX_PASSWORD=your-password
# Recovery key for E2EE (optional, for key backup)
MATRIX_RECOVERY_KEY=

# =============================================================================
# Matrix Direct (optional - for direct access token auth)
# =============================================================================
MATRIX_ACCESS_TOKEN=syt_xxxxx

# =============================================================================
# Gateway URL (required for HTTPS/WebAuthn)
# =============================================================================
# Full URL with scheme - REQUIRED when tailscale.https is enabled
# This is used for WebAuthn/passkeys and admin invite links
FOLD_GATEWAY_URL=https://fold-gateway.your-tailnet.ts.net

# Or just the hostname (for fold-admin CLI, defaults to http://)
FOLD_GATEWAY_HOST=fold-gateway.your-tailnet.ts.net

# =============================================================================
# Admin CLI (optional)
# =============================================================================
# JWT token for fold-admin CLI (get via: fold-admin token create)
FOLD_TOKEN=eyJhbG...
